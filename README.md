# myTools
A collection of program, tools ad miniAPP

The idea is to have a uniq repository of different code, example and codelet for HPC stuff
Each directory is a different "tool".

Up today, these are the different "tools"

* NVML_example: three fortran-based programs with an interface tp nvml to track GPU energy.
