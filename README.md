# myTools
A collection of program, tools ad miniAPP

The idea is to have a unique repository of different code, example and codelet for HPC stuff
Each directory is a different "tool".

Up today, these are the different "tools" available

* NVML_example: three fortran-based programs with an interface to nvml to track (only) GPU energy.
